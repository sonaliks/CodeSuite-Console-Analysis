version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install pyyaml boto3
  build:
    commands:
      - echo "Validating LZA accounts-config.yaml..."
      - python validate_ou_names.py
