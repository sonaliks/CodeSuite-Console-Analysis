# Scenario 1 Demo App

A simple web application deployed via CodeDeploy.

## Note

This repository intentionally does NOT include an `appspec.yml` file,
which will cause the CodeDeploy deployment to fail.
