# Common dependencies shared across all Python components
# For component-specific dependencies, see individual requirements.txt files
# in mcp-servers/codecommit/, mcp-servers/codepipeline/, backend/, and infrastructure/

mcp>=1.0.0
boto3>=1.34.0
pydantic>=2.5.0
